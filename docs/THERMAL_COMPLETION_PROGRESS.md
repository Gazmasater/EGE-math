# Завершение всех 140 задач — журнал проверки

Запрос пользователя: «доделывай все 140». Работу не завершать после частичного пакета. Прежние 17 решений раздела и все 583 строки базы должны остаться неизменными.

Рабочие артефакты: `/root/thermal-complete-Sg3yhl`. Резервная база `before.sqlite`, контрольные суммы `baseline.json`. Все 123 новые задачи опубликованы 21.09.2026 в 17:43 UTC; в разделе готовы 140 из 140.

Состав полностью прочитан: 123 оставшихся условия. Все 123 решения написаны и подключены к каноническому набору: газы (21), влажность (17), испарение (4), поршни (16), ртуть (5), плавание (8), смешанная механика (6), качественные графики (13), построения (10), циклы (13), заключительные задачи (10). У каждой задачи собственный вывод, описание независимой сверки, SVG и независимая проверка расчёта или качественных отношений. Контентный аудит 139 канонических записей проходит; прежняя F717A0 дополнительно проверяется без изменений.

Сохранённые независимые страницы и поисковые соответствия: `references.json` (595 задач), `matches.json`; автоматические соответствия — только подсказки, не подтверждение сверки. Скрипт `thermal-source-inventory.js` загружает общедоступные подборки и строит кандидатов. Оригиналы ФИПИ не менялись. Онлайн-банк ФИПИ 21.09.2026 отвечает ошибкой/тайм-аутом.

Оригинальные рисунки сведены в `originals-01..11.html/png` для просмотра (`thermal-source-sheets.js`). Все 11 листов визуально просмотрены. Все исходные растры сохранены внутри окончательных SVG; аудит проверяет их исходные байты. В механических задачах силы нанесены на исходную геометрию. Построения p–V и V–T выполнены отдельно от неизменённых оригиналов.

Особые проверки:

- 225695: при охлаждении в жёстком сосуде до росы постоянна плотность, а не давление пара. Начальное p≈3001 Па при 325 К; точка росы определяется пересечением pн(t) и ρR(t+273)/M. По рисунку около 22 °C и 27 гПа; явно указана модель закрытого жёсткого сосуда и точность графического считывания. Независимая сверка — методический аналог 42650, не точное числовое совпадение.
- 4E7BA7: на картинках в условии ν=4 моль, Q нагревателя=120 кДж. В текстовом извлечении эти числа отсутствуют.
- 3B1100: по именно заданному графику pн(16 °C)≈20 гПа, не подменять график реальной справочной таблицей; источник 42650 даёт долю 0,0075.
- D6F721: в источнике 25422 опечатка в обозначении полного/парциального давления; проверено вручную, здесь p≈140042 Па. Степень закона распада оформлена типографически через безразмерное x=τ÷θ.
- В отдельных пояснениях используется знак деления ÷; вычислительные дроби оформлены вертикально. Браузерный аудит не обнаружил неотрисованных разделителей дробей или выхода текста за границы.

Канонический аудит расширен до 139 новых/пакетных + прежней F717A0 = 140; проверены вся тема, все страницы 1280/390 px, API и исходные картинки. Публикация идемпотентна: `changed=123`, повторно `changed=0`. Все 583 прежние строки и шесть исходных HTML не изменились. После окончания публичных проверок удалены только собственные ненужные временные PNG и неактивные профили браузера; манифесты сохранены.

Предпросмотр: `/root/thermal-complete-Sg3yhl/preview`, в его отдельной базе 706 опубликованных решений; все 583 прежние строки полностью совпали с резервной базой. Пилотный аудит 12 страниц × 2 размера + 12 SVG и полный прогон 140 страниц × 2 размера + 140 SVG завершились без ошибок. Все 35 обзорных листов страниц (компьютер и телефон рядом) просмотрены до публикации.

Просмотрены все 35 контактных листов схем (140 задач). По результатам исправлены подписи сил, положение подписи подъёма пузырька, вертикальная ось у пробки и масштабы маленьких исходных формул 4E7BA7. У AE2D32 снаружи вакуум: убрана ошибочная внешняя сила давления в собственной схеме; текст и расчёт уже учитывали вакуум правильно. Исправленные SVG повторно проверены в браузере и просмотрены крупно. У пробок 18E99E и 1D4573 уточнено вводное описание модели с учётом трения. Общий аудит физики на предпросмотре и рабочей базе проходит; `audit:solutions` проверил формат всех 706 решений и численные корни математических задач. Из-за зависания большого прогона снимков браузер запускается группами по 35 задач, отчёт сохраняется после каждой страницы; закрытие собственного Chrome и удаление его профиля выполняются с ожиданием завершения.

После перезапуска только `ege-math.service` все 706 публичных API вернули HTTP 200; ответы, решения, SVG и подписи совпали с рабочей SQLite. Повторный публичный браузерный аудит завершён: 280 уникальных просмотров, 140 SVG, 140 отметок публикации каталога, 0 ошибок. Дополнительно просмотрены восемь публичных страниц. SEO: 983 задачи, 29 каталогов, 1013 URL sitemap, 706 решений, 0 ошибок. `audit:physics`, `audit:solutions`, три теста SEO и шесть тестов классификации пройдены.

Реестр независимой сверки всех 139 тепловых записей: 51 точное совпадение, 62 варианта, три обратные задачи и 23 методических аналога. У каждой записи явно указаны источник и существенные различия. Для F717A0 сохранены прежние источник и решение из набора колебаний. Итоговые ответы — `scripts/data/thermal-answers.json`; сверка — `thermal-sources.json`; состав раздела — `thermal-catalog.json`.

Протокол публикации — `publication.json`; полные отчёты — `all-pages/combined-report.json`, `public-pages/combined-report.json`, `public-api-report.json`, `seo-live/seo-report.json`. Просмотр схем и обзорных листов зафиксирован в `visual-review.json`. Все 960 временных PNG этой работы удалены после проверок (308 559 623 байта); перечень, размеры и SHA-256 — `screenshot-cleanup-manifest.json`. Ещё удалены два собственных неактивных профиля Chrome и тестовая HTML-выкладка (223 225 795 байт), перечень — `temporary-profile-cleanup.json`. Исходные картинки ФИПИ, SVG, исходники, резервная и рабочая базы, отчёты не удалялись. Скриншоты можно создать заново скриптами проверки; архивных копий PNG нет.

Запрос выполнен полностью: 140 из 140 оформлены и опубликованы, незавершённых задач этого раздела нет.
