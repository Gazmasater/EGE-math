# Безопасная очистка сервера — 21.09.2026

Очистка выполнена по прямому запросу пользователя. До очистки доступное место на корневом разделе было 0 байт, после — 1 222 029 312 байт (около 1,2 ГБ); заполнение снизилось со 100% до 95%. Значения сняты во время работы сервисов и могут меняться.

## Что удалено

| Категория | Объём до очистки | Ограничения |
| --- | --- | --- |
| Шесть временных профилей проверок физики | 277.1 МиБ | Проверены реальные пути, владелец, структура профиля; нет использующих процессов и открытых файлов |
| Кэш APT | около 135 МиБ | Штатный `apt-get clean`; установленные пакеты не удалялись |
| Кэш npm | около 179 МиБ | Штатный `npm cache clean --force --cache /root/.npm`; зависимости проектов не тронуты |
| Кэш pip | около 26 МиБ освобождено | Штатный `pip3 cache purge`; удалено шесть файлов, установленные пакеты сохранены |
| Кэш скачанных CRX-компонентов браузера | около 306 МиБ | Только `/var/lib/holod48-vk-token-browser/component_crx_cache`; установленные компоненты и профиль сохранены |

Точные временные профили:

- `/tmp/statics-browser-D7zamT`
- `/tmp/statics-browser-WhEShw`
- `/tmp/statics-browser-PcoC6j`
- `/tmp/statics-browser-GnwRDO`
- `/tmp/statics-browser-1Z9SqJ`
- `/tmp/dynamics-browser-r0lMvd`

Удалённые временные профили не архивировались: новые профили создаются при следующем запуске проверок. Кэши скачиваются заново при необходимости. Файлы кэша CRX проверены по сигнатуре; до и после удаления сопоставлены 3876 файлов и ссылок остальной части профиля. Содержимое обычных файлов проверено по SHA-256, сохранены владельцы и права. Cookies, данные входа, сеансы, настройки и установленные компоненты не изменены.

## Сжатие без потери данных

| Исходный файл | Сохранённый архив | До | После |
| --- | --- | --- | --- |
| `/var/log/btmp.1` | `/var/log/btmp.1.gz` | 125.97 МиБ | 3.09 МиБ |
| `/var/log/syslog.1` | `/var/log/syslog.1.gz` | 42.96 МиБ | 2.52 МиБ |
| `/var/log/auth.log.1` | `/var/log/auth.log.1.gz` | 33.69 МиБ | 2.57 МиБ |

Освобождено 194.4 МиБ. Сжимались только закрытые ротированные журналы с одной жёсткой ссылкой. Сначала созданы архивы с сохранением исходников, выполнены `gzip -t` и сравнение SHA-256 распакованных данных с оригиналами, повторно проверены оригиналы и отсутствие открытых дескрипторов. Только затем удалены несжатые копии. Архивы сохраняют содержимое, владельцев, права и время изменения; исходные записи можно полностью восстановить распаковкой.

Текущие журналы, файлы journald, lastlog и активный btmp не очищались.

## Что не менялось

- Рабочие проекты, исходники, node_modules, базы данных, резервные копии, исходные выгрузки ФИПИ.
- Итоговые отчёты и скриншоты всех аудитов.
- История диалогов, созданные изображения и установленные инструменты Codex.
- Установленные системные пакеты, Snap-ревизии, настройки служб и правила хранения журналов.
- Сервисы не перезапускались и не останавливались.

## Проверки после очистки

- `ege-math.service` и `nginx`: active.
- Главная страница и `/physics?topic=1.4`: HTTP 200.
- `npm run audit:physics`: все 117 решений корректны, изменений базы нет.
- `npm run audit:solutions`: все 562 опубликованных решения корректны, численная проверка корней пройдена.
- В проекте сохранён прежний рабочий diff; в этом изменении добавлены только отчёт очистки и запись в контексте проекта.

## Замечания первичной диагностики

- `vk-token-refresh.service` находился в failed ещё до очистки (последнее завершение 21.09.2026 03:30:45 UTC). Служба, её настройки и данные входа не изменялись; исправление авторизации не входило в очистку.
- До очистки `journalctl --disk-usage` сообщил об усечённом `system.journal`. Файлы journald не удалялись и не переписывались в рамках этой работы.
- Диск всё ещё заполнен примерно на 95%. Перед следующими крупными сборками и браузерными аудитами проверять свободное место. Более глубокую очистку истории, резервных копий, установленных компонентов или данных приложений автоматически не выполнять.

## Дополнительное удаление скриншотов — 21.09.2026

После описанной выше очистки пользователь отдельно разрешил удалять ненужные скриншоты. Удалены 935 PNG завершённых проверок, не используемых кодом сайта:

| Каталог | Удалено файлов |
| --- | ---: |
| `/root/statics-audit-artifacts` | 96 |
| `/root/kinematics-audit-artifacts` | 73 |
| `/root/dynamics-audit-artifacts` | 377 |
| `/root/conservation-audit-artifacts` | 301 |
| `/root/seo-audit-artifacts` | 68 |
| Корень `/root/projects/ege-math` — только 20 проверенных PNG | 20 |

Освобождено 272 719 872 байта занятого места (около 273 МБ). После удаления доступно 1 475 051 520 байт (около 1,4 ГиБ), заполнение 93%; свободное место меняется во время работы сервисов.

Перед удалением проверены точные пути, сигнатуры PNG, отсутствие символических и дополнительных жёстких ссылок, работающих браузерных аудитов и открытых дескрипторов выбранных файлов. Ни один из 20 PNG корня проекта не отслеживался Git; ссылок на них в рабочих файлах проекта не найдено. Каждый файл удалён отдельно по подготовленному перечню после повторной проверки размера, inode и SHA-256. Каталоги целиком не удалялись.

Сохранены все исходные изображения ФИПИ, изображения документации, SVG решений, базы и резервные копии, JSON/HTML/PDF-отчёты и скрипты проверок. Единственный оставленный PNG в каталогах аудитов — `/root/statics-audit-artifacts/iron-primary.png`: это снимок страницы методического журнала для сверки условия и решения, а не снимок интерфейса сайта. До и после удаления совпали SHA-256 всех 806 защищённых файлов и полного содержимого 562 записей решений.

Перечень удалённых файлов, их размеры и контрольные суммы, проверяемые сохранённые файлы и результаты записаны в `/root/conservation-audit-artifacts/screenshot-cleanup-2026-09-21.json` (статус `complete`). Скриншоты не архивировались и не могут быть восстановлены из этого перечня. Исторические ссылки на них в отчётах теперь указывают на удалённые файлы; сами отчёты сохранены. Повторный браузерный аудит создаёт новые снимки текущего состояния, не восстанавливая прежнее. Обязательные браузерные проверки и визуальный просмотр перед публикацией остаются в силе; автоматическая очистка не устанавливалась.

После удаления повторно пройдены `npm run audit:physics` (117 решений) и `npm run audit:solutions` (562 решения), `/physics?topic=1.4` возвращает HTTP 200, `ege-math.service` и `nginx` активны. Службы не перезапускались; код сайта и данные решений в рамках удаления скриншотов не менялись.

## Выборочное удаление компиляторов C/C++ — 21.09.2026

Пользователь согласовал удаление gcc/g++, сообщил, что запуск eBPF пока не требуется, и отдельно указал сохранить написанные им файлы. Удалены только пакеты APT: `build-essential`, `gcc`, `g++`, `gcc-11`, `g++-11`, `libgcc-11-dev:amd64`, `libstdc++-11-dev:amd64`. Пользовательские исходники, проекты, файлы C/C++/eBPF, данные, резервные копии и инструменты eBPF не удалялись.

Перед удалением `dpkg --verify` не обнаружил изменений проверяемых файлов этих пакетов; их перечни не содержали пользовательских каталогов. Повторная симуляция APT подтвердила ровно семь удалений без дополнительных пакетов. Выполнено `apt-get remove` с явным списком и отключённым `AutomaticRemove`; `autoremove`, очистка библиотек и удаление файлов вручную не выполнялись. Автоматический перезапуск служб не разрешался.

Размер удалённых пакетов — 114 379 КиБ, или 111,7 МиБ (APT: 117 МБ). Доступное место выросло с 1 336 086 528 до 1 454 632 960 байт, заполнение 93%; разница показаний включает текущую активность сервера. Остальные 912 установленных пакетов сохранили версии, новых пакетов не установлено. Сохранены `libc6`, `libgcc-s1`, `libstdc++6`, препроцессоры `cpp`/`cpp-11` и `x11-xserver-utils`: удаление препроцессора потянуло бы графические утилиты и было исключено из плана.

`dpkg --audit` не выявил проблем. Повторные `audit:physics` (117) и `audit:solutions` (562) пройдены, публичный `/physics?topic=1.4` отвечает HTTP 200. SHA-256 `server.js` и полного содержимого 562 записей решений не изменились. `ege-math.service` и `nginx` активны с прежними PID, без перезапуска.

Запись с версиями удалённых пакетов, результатами проверки и замерами: `/root/conservation-audit-artifacts/compiler-removal-2026-09-21.json`, статус `complete`. Компиляторы можно установить заново командой `apt-get install build-essential`; точные прежние версии при этом не гарантируются. До восстановления сборка некоторых нативных зависимостей других проектов может быть недоступна. Отсутствие компиляторов не является разрешением удалять пользовательские исходники или файлы eBPF.
